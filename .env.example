# Copy this file to .env.local and fill in your values when you connect
# the lead form to a real email service (see app/api/lead/route.js).

# Example if using Resend:
# RESEND_API_KEY=
# LEAD_EMAIL_TO=

# Example if using SMTP / Nodemailer:
# SMTP_HOST=
# SMTP_PORT=
# SMTP_USER=
# SMTP_PASS=
